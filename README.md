

# 运行
```bash
# 构建并启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

# 访问

- frondend: http://host/
- backend: http://host/api
- Grafana: http://host/grafana
